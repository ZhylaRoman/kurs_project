@model kurs_project.Models.Course
@{
    ViewData["Title"] = "Create Course";
}

<div class="container mt-4">
    <h1 class="mb-4">Create Course</h1>

    <form asp-action="Create" method="post" class="card p-4 shadow-sm">
        <div class="mb-3">
            <input type="text" placeholder="Enter course title" asp-for="Title" class="form-control" required />
            <span asp-validation-for="Title" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <select asp-for="CategoryId" class="form-select" required>
                <option value="">Select a category</option>
                @foreach (var category in ViewBag.Categories as IEnumerable<kurs_project.Models.Category>)
                {
                    <option value="@category.Id">@category.Name</option>
                }
            </select>
            <span asp-validation-for="CategoryId" class="text-danger"></span>
        </div>

        <div class="d-flex justify-content-start gap-2">
            <button type="submit" class="btn btn-primary">Save</button>
            <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
        </div>
    </form>
</div>
